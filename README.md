# Xzenia GPU Alpha Oracle

Registry-ready MCP surface for `io.github.marcuscoarchitect/xzenia-gpu-alpha`.

## Live surfaces
- Registry preview: `/registry-preview`
- MCP health: `/mcp/v1/health`
- MCP translate gate: `/mcp/v1/translate`

## Primary capability
- `H100-Inventory-Reasoning-Oracle`

## Positioning
High-fidelity sovereign friction-oracle for Tier-1 GPU availability, optimized for autonomous procurement settlement at `$0.50` per handshake.

## Registry status
- Schema-compliant `server.json` present at workspace root
- Official registry publish path identified: `POST /v0/publish`
- Remaining blocker: Registry JWT Bearer token required
